the problem with functions

<!-- noop and identity -->

map
function signature

function sum (x) {
  return function (y) {
    return x + y;
  }
}

<!-- niave map -->

<!-- export const getRetailerPage = partial(getPage, ); -->
